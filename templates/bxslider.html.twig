<div id="{{ settings.slider_id }}">
  <ul class="bxslider" style="">
    {%  for slide in slides %}
      <li>{{ slide.image }}</li>
    {%  endfor %}
  </ul>
  {%  if settings.slider_settings.pagerCustom_type and settings.slider_settings.pagerCustom_type == 'thumbnail_pager_method1' %}
    <div id="{{ settings.slider_settings.pagerCustom }}">
      {% for key, item in items %}
        <a data-slide-index="{{ key }}" href="">{{ item.slide_pagerCustom }}</a>
      {%  endfor %}
    </div>
  {%  endif %}
</div>
